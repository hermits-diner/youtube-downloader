# Dependencies & build output
node_modules/
dist/

# Local logs / temp
*.log
_el_*.log
build/_preview.png

# Claude Code / MCP local artifacts (not project content)
.remember/
.playwright-mcp/

# OS
Thumbs.db
Desktop.ini
.DS_Store

# NOTE: bin/ (yt-dlp.exe, ffmpeg.exe) is intentionally committed so the
# project is self-contained and buildable right after clone.
